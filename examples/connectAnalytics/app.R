library(connectAnalytics)
library(shiny)
# Need to set the environment variables on the server
# Sys.setenv(CONNECT_SERVER = "https://rstudio-connect.heor.co.uk",
#            CONNECT_API_KEY = "YOUR_API_KEY")
# Run
connectAnalytics()
